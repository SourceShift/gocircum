canary_domains:
  - "www.example.com"

fingerprints:
  - id: "test_tcp_utls"
    description: "Test TCP with uTLS (Randomized)"
    transport:
      protocol: "tcp"
    tls:
      library: "utls"
      client_hello_id: "HelloRandomized"
      min_version: "1.3"
      max_version: "1.3"
      skip_verify: false 