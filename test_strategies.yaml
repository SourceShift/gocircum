canary_domains:
  - "www.example.com:443"

fingerprints:
  - id: "test_tcp_stdlib"
    description: "Test TCP with stdlib TLS 1.3"
    transport:
      protocol: "tcp"
    tls:
      library: "go-stdlib"
      min_version: "1.3"
      max_version: "1.3"
      skip_verify: true 